import styled from 'styled-components';

const Main = styled('div')`
  & .props-form-item {
    margin-bottom: 0;
  }
  
  & .ant-upload.ant-upload-select {
    display: block;
  }
`;

export { Main };
