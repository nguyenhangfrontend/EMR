import styled from 'styled-components';

const Main = styled('div')`
  padding: 3px;
  border: solid 1px rgba(0, 0, 0, 0.4);
  border-radius: 4px;
  background: #fff;
  width: 180px;
  
  & .tool-btn {
    margin: 0 6px;
  }
`;

export { Main };
